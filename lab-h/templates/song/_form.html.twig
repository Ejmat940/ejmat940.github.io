{{ form_start(form) }}
    {{ form_widget(form) }}
    {# Użyj klasy Bootstrap, ustaw type=submit i dodatkowy odstęp #}
    <button type="submit" class="btn btn-primary mt-3">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
